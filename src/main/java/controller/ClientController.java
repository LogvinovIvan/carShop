package controller;import dao.exception.DAOException;import dao.exception.ServiceException;import dao.factory.DAOFactory;import dao.implement.CarDAO;import dao.implement.ClientDAO;import entity.Car;import entity.Client;import service.AddService;import service.DeleteService;import service.UpdateService;import javax.servlet.RequestDispatcher;import javax.servlet.ServletException;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.io.IOException;import java.util.List;/** * Created by evgen on 08.04.2016. */public class ClientController extends HttpServlet {    protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        Client client;        switch (request.getParameter("request")){            case "add_client":                client = new Client(request.getParameter("firstName"),                        request.getParameter("lastName"),                        request.getParameter("phoneNumber"));                try {                    AddService.add(client,"client");                } catch (ServiceException e) {                }                break;            case "delete_client":                client = new Client();                client.setId(Integer.parseInt(request.getParameter("idWorkerOnDelete")));                try {                    DeleteService.delete(client,"client");                } catch (ServiceException e) {                }                break;            case "update_client":                client = new Client(request.getParameter("firstName"),                        request.getParameter("lastName"),                        request.getParameter("phoneNumber"));                client.setId(Integer.parseInt(request.getParameter("id")));                try {                    UpdateService.update(client,"client");                } catch (ServiceException e) {                }                break;        }    }    protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        //ClientDAO dao = DAOFactory.getClientDAO();        /*try {            List<Client> clientList = dao.getAll();            request.setAttribute("clientList", clientList);            RequestDispatcher view = request.getRequestDispatcher("/client.jsp");   /////////////////            view.forward(request, response);        } catch (DAOException e) {            RequestDispatcher view = request.getRequestDispatcher("/error.jsp");            view.forward(request,response);        }*/    }}