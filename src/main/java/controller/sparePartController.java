package controller;import dao.exception.DAOException;import dao.exception.ServiceException;import dao.factory.DAOFactory;import dao.implement.CarDAO;import entity.Car;import entity.SparePart;import service.AddService;import service.DeleteService;import service.UpdateService;import javax.servlet.RequestDispatcher;import javax.servlet.ServletException;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.io.IOException;import java.util.List;/** * Created by evgen on 08.04.2016. */public class SparePartController extends HttpServlet {    protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        SparePart sparePart;        switch (request.getParameter("request")){            case "add_spare":                sparePart = new SparePart(request.getParameter("name"),                        request.getParameter("mark"),                        request.getParameter("model"),                        request.getParameter("articul"));                try {                    AddService.add(sparePart,"spare");                } catch (ServiceException e) {                }                break;            case "delete_spare":                sparePart = new SparePart();                sparePart.setNumberSpareParte(Integer.parseInt(request.getParameter("idSpareOnDelete")));                try {                    DeleteService.delete(sparePart,"spare");                } catch (ServiceException e) {                }                break;            case "update_spare":                sparePart = new SparePart(request.getParameter("name"),                        request.getParameter("mark"),                        request.getParameter("model"),                        request.getParameter("articul"));                sparePart.setNumberSpareParte(Integer.parseInt(request.getParameter("id")));                try {                    UpdateService.update(sparePart,"spare");                } catch (ServiceException e) {                }                break;        }    }    protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        //SparePartDAO dao = DAOFactory.getSparePartDAO();        /*try {            List<SparePart> sparePartList = dao.getAll();            request.setAttribute("sparePartList", sparePartList);            RequestDispatcher view = request.getRequestDispatcher("/storeKeeper.jsp");   /////////////////            view.forward(request, response);        } catch (DAOException e) {            RequestDispatcher view = request.getRequestDispatcher("/error.jsp");            view.forward(request,response);        }*/    }}