package controller;import dao.AbstractDAO;import dao.exception.DAOException;import dao.exception.ServiceException;import dao.factory.DAOFactory;import entity.Car;import entity.StorageCar;import service.AddService;import service.DeleteService;import service.UpdateService;import javax.servlet.RequestDispatcher;import javax.servlet.ServletException;import javax.servlet.annotation.WebServlet;import javax.servlet.http.HttpServlet;import javax.servlet.http.HttpServletRequest;import javax.servlet.http.HttpServletResponse;import java.io.IOException;import java.util.List;/** * Created by evgen on 08.04.2016. */@WebServlet(name = "storageCar")public class StorageCarController extends HttpServlet{    protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {       StorageCar storageCar;        switch (request.getParameter("request")){            case "add_car":                storageCar = new StorageCar(Integer.parseInt(request.getParameter("vinCode")),                        request.getParameter("status"));                try{                    AddService.add(storageCar, "storageCar");                }catch(ServiceException ex){                    response.getWriter().append(ex.getMessage());                }                break;            case "delete_car":                try {                    storageCar=new StorageCar();                    storageCar.setVin(Integer.parseInt(request.getParameter("vinCode")));                    DeleteService.delete(storageCar, "storageCar");                } catch (ServiceException e) {                }                break;            case "update_car":                storageCar = new StorageCar(Integer.parseInt(request.getParameter("vinCode")),                        request.getParameter("status"));                try {                    UpdateService.update(storageCar, "storageCar");                } catch (ServiceException e) {                }                break;        }    }    protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {        DAOFactory daoFactory = new DAOFactory();        AbstractDAO dao = daoFactory.getDao("storageCar");        try {            List<StorageCar> storageCarList = dao.findAll();            request.setAttribute("storageCarList", storageCarList);            RequestDispatcher view = request.getRequestDispatcher("/customerServiceSpecialist.jsp");   ///////////////////////////////            view.forward(request, response);        } catch (DAOException e) {            RequestDispatcher view = request.getRequestDispatcher("/error.jsp");            view.forward(request,response);        }    }}